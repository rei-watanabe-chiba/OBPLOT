# OBPLOT タブ2（グラフ描画・データ補正機能）設計書

## 1. 全体アーキテクチャ・状態管理
- **フェーズ管理**: Tab1とは独立した `TAB2_PHASE` を新設し、アプリの進行状態を管理する。
  - `INIT`: 初期状態
  - `LOADED`: データ読込完了
  - `FILTERED`: データ設定（絞り込み）完了
  - `MAPPED`: シンボルマッピング完了
- **データモデル変換**: 
  - `baseData`（ベースデータ）までは**行指向**のオブジェクト配列として保持（フィルタリング等の行単位処理を容易にするため）。
  - `selectData`（計算・プレビュー用データ）生成時など、計算エンジンに渡す直前に**列指向**へ変換し、配列同士の演算を最適化する。

## 2. Sidebar UIの設計（アコーディオン4層構造）
`<details>`を用いたアコーディオンで各エリアを構成。各エリアUIはPhase進行に応じて動的に生成・更新する。

### ① データ設定（ConfigArea）
- **機能**: 外部データの読み込みと、分析対象のdataset絞り込み。
- **UI**: 「読込」ボタン
  - 押下時、バックエンドから `PXRF`, `WDXRF`, `Correction` の3シートをロードする。
  - `PXRF` または `WDXRF` が空の場合はエラーとして進行不可。
  - `Correction` が空の場合は正常とし、以降の補正選択肢を「補正なし」「現在の補正値」のみに制限する。
- **UI**: 読込後、PXRFの `dataset` をリストアップした「スクロール可能なチェックボックス群」を動的生成。
- **UI**: 「確定/更新」ボタン
  - チェックボックス入力後、`Tab2ST.filter` に状態を保存し、対象データを絞り込む。
  - ※UI更新・ロジックの完全分離（MVC+Method分離）を徹底する。

### ② シンボルマッピング（SymbolMapArea）
- **機能**: グラフ上の点（マーカー）のスタイル設定。
- **UI**: マーカ分類軸選択プルダウン（`dataset`, `item`, `Group`, `Source`）。
- **UI**: 10行の設定行（[値名] + [形状: `circle`, `triangle`, `square`] + [色]）。
  - 値名はbaseKeyに応じて該当列のユニーク値を動的生成。
- **UI**: 「シンボル確定」ボタン。`Tab2ST.symbol` にマッピング。

### ③ 補正＆プレビュー（CorrectionArea）
- **機能**: 特定元素・指数の相関確認と補正適用・出力。
- **描画エンジン**: Sidebar内で Google Visualization API を使用してプレビュー用散布図を描画。
- **UI**: [使用モード]: 各元素（Mn, Fe, Rb, Sr, Y, Zr, Nb）で "obsidian" / "mudrock" を選択。
- **UI**: [対象]: `GLB.valueLogic`（単純な計算式の文字列配列、例: `["Mn", "Mn * 100 / Fe"]`）から選択。
- **UI**: [補正値]: 「補正なし」「現在の補正値」「Correctionシートのインデックス名マップ」から選択。
- **UI**: [プレビュー更新]ボタン
  - 押下時、データを列指向に変換して数式コンパイラによる一括計算を実行し、グラフおよび統計情報パネル（相関係数、傾き等）を動的レンダリング。
- **UI**: [新規インデックス名入力欄] + 「Correction保存」ボタン。

### ④ レポート出力設定（ReportArea）
- **機能**: 複数元素・指数の一括レポート生成。
- **UI**: レポート・スロット (1〜9行)。各行で `GLB.valueLogic` から対象を選択。
- **UI**: 「レポート表示」ボタン。
  - 選択された全ての計算式に対して列指向で一括計算（`finalData` 生成）を行い、別タブ（Report.html）でグラフを描画。

## 3. コード設計（MVC + Method 分離）
- **列指向データモデル**: 前述の通り、計算処理の直前で列指向に変換。
- **安全な動的式評価**: `eval` を避け、`new Function()` を用いたセキュアで高速な配列一括計算コンパイラを実装する。
- **関心分離**: `DataMerger` 等のMethod層はビジネスロジックに純粋化し、Controllerからの呼び出しとStateの更新を明確に分離する。

### 【State (Model) 】
- `GLB` 拡張
  - `sheetNames`: `{ pxrf, wdxrf, correction }` 等
  - `valueLogic`: `["Mn", "Fe", "Mn * 100 / Fe", ...]` (ラベル兼数式の文字列配列)
- `Tab2ST`
  - `phase`: `TAB2_PHASE` による状態管理
  - `rawData`: 各シートの生データ
  - `baseData`: IDで結合した行指向ベースデータ
  - `selectData`: 計算適用後の列指向可視化データ
  - `filter`, `symbol`, `modeMap`, `preview`, `correctionLogic`, `report`, `finalData` 等（状態と一時データを分離管理）
