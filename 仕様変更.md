# 設計変更
  - データ属性（dataset, itme, date）をそのまま抽出層に移動する。位置はデータ抽出の下、データ格納の上
  - gnfigbarはモード切替のテキスト変更のみに依存
  - UI操作に関わるstateは以下だけにする。
    - 各層のstatus：ステータスバーのみと連動
    - file.isMapError, exac.isMapError(configから移動変更）：セレクタ・インプットUI更新とボタン有効化判定に連動
    - Tab1ST.phase：Tab1の処理進捗状態を記録
    * geminiへの要望： Tab1ST.phase は進行状態名と数字が連動するようにしたい。UIControllerの改修仕様をみて最適な方法を複数提案して
  - UIControllerの各エリアの操作を、phaseとisMapErrorに対応するように改修

# UIController改修
## 初期化（phase = init, 1)
初期状態で設定するためUIController との連動はなし。 
* geminiへの要望： 初期化操作にstate操作を組み込んで、UIController を発火した方がいい場合が想定できるならば提案してください。
### 設定層：
  - モード切替が有効化
### ファイル層：
  - データ取得ボタン無効
  - 列選択マップ、リスト生成ボタン、検証ボタンは非表示
### 抽出層：
　- データ抽出ボタン無効
　- データ属性マップ、データ格納ボタンは非表示

## データ取得成功（phase = load ,2) 
　データ取得により内部データがリセットされるため、処理進行後もデータが再取得されれば、UIは毎回リセットする設計とする
・設定層：変化なし
・ファイル層：
　列選択初期化（表示、open、選択肢初期化）
　リスト生成・検証ボタンは表示、無効化
・抽出層：
　データ抽出ボタン無効化
　データ格納ボタンは非表示 
　データ属性マップ（close, 無効化）
　* geminiへの要望：phase x 以前から移行して表示されてない場合もcolseしてエラーが出るかどうか判断して。必要ならば条件分岐実行を提案して 

# データ要検証（phase =  Invalid ,3)
　リスト生成時と検証時にaleartの時に発火する
　・ファイル層
　　列選択 close、検証ボタン有効化
　・抽出層
　　
        

#  列選択変化（file.isMapErrorのsomeがtrue / false)
　trueの場合（エラー含む） 
　・ファイル層： 
　　リスト生成ボタン無効化 
　　phase 2 以下ならば検証ボタン無効化
　falseの場合（エラー含まない） 
　・ファイル層： 
　　リスト生成ボタン有効化


#  編集（phase = edited, 2）
*データ抽出ボタン押下時に編集フラグ検知で発火
 



