# for gemini
# Specification Change Design Document(dft)
- **この設計変更はまだ企画段階です。内容の検証とユーザーとの対話により変更内容を補足・確定する必要があります。**
- データ属性（dataset, itme, date）をそのまま抽出層に移動する。位置はデータ抽出の下、データ格納の上
- gnfigbarはモード切替のテキスト変更のみに依存
- UI操作に関わるstateは以下だけにする。
  - file.status, exac.status：ステータスバーのみと連動
  - config.mode：ステータスバーとphaseに連動
  - file.isMapError, exac.isMapError(configから移動変更）：セレクタ・インプットUI更新とボタン有効化判定に連動
  - Tab1ST.phase：Tab1の処理進捗状態を記録
- UIControllerの各エリアの操作を、phaseとisMapErrorに対応するように改修
- EventManagerを仕様変更に対応するように改修（大きな改修は設定層分離による変化、抽出層ロジックの追加）
- geminiへの要望：Tab1ST.phase は進行状態名と数字が連動するようにしたい。UIControllerの改修仕様をみて最適な方法を複数提案して

# UIController改修
## 初期化（phase = init, 1)
初期状態で設定するためUIController との連動はなし。 
- geminiへの要望：初期化操作にstate操作を組み込んで、UIController を発火した方がいい場合が想定できるかどうか判断して。
- **設定層**：モード切替が有効
- **ファイル層**：データ取得ボタン無効。列選択マップ、リスト生成ボタン、検証ボタンは非表示
- **抽出層**：データ抽出ボタン無効。データ属性マップ、データ格納ボタンは非表示

## データ取得成功（phase = load ,2) 
データ取得時に変更。処理進行後もデータが再取得されれば、UIは毎回リセットする設計とする
- **設定層**：変化なし
- **ファイル層**：列選択初期化（表示、open、選択肢初期化）。リスト生成・検証ボタンは表示・無効化
- **抽出層**：データ抽出ボタン無効化。データ格納ボタンは無効化。データ属性マップ（close, 無効化）
  - geminiへの要望：phase 4 未満から移行した（データ格納とデータ属性が表示されてない）場合、抽出層操作をしてエラーが出るかどうか判断して。

# データ要検証（phase =  Invalid ,3)
リスト生成・検証時でaleartの時に変更。
extractFileで編集検知時とconfig.mode変更時には（phase =  Invalid ,3)に強制移行。
- **ファイル層**：列選択close。検証ボタン有効化。
- **抽出層**：データ抽出ボタン無効化。データ格納ボタン無効化。データ属性マップ（close, 無効化）
  - geminiへの要望：phase 4 未満から移行した（データ格納とデータ属性が表示されてない）場合、抽出層操作をしてエラーが出るかどうか判断して。

# データ検証済み（phase =  valid ,4)
リスト生成・検証時でsuccessの時に発火する。
- **ファイル層**：列選択close。検証ボタン有効化。
- **抽出層**：データ抽出ボタン判定（exac.isMapErrorのsomeがfalse）。データ格納ボタン表示・無効化。データ属性マップ（表示, open, 有効化）

# データ抽出済み（phase =  Extract ,5)
データ抽出がsuccessの時に発火する
- **ファイル層**：列選択close。
- **抽出層**：データ格納ボタン有効化。データ属性マップ（close）   


# 列選択変化（file.isMapErrorのsomeがtrue / false)
- **true**の場合（エラー含む）
  - **ファイル層**：リスト生成ボタン無効化。phase 2 以下ならば検証ボタン無効化
- **false**の場合（エラー含まない）
  - **ファイル層**：リスト生成ボタン有効化

# データ属性変化（exac.isMapErrorのsomeがtrue / false)
- **true**の場合（エラー含む）
  - **抽出層**：データ抽出ボタン無効化
- **false**の場合（エラー含まない）
  - **抽出層**：データ抽出ボタン有効化
