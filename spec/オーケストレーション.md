プロンプト生成
/workflow generate-prompt
アプリ開発
/workflow gas-orchestration


.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: 新機能の設計および実装計画の策定】
追加要件:
- 対象機能: 【例: Tab2に〇〇のデータ集計およびグラフ描画オプションを追加】
- 期待する振る舞い: 【例: ユーザーが〇〇を選択した際、AppStateの派生状態として計算し、バインディングを通じてプレビューを更新する】
- 入出力仕様: 【例: 入力: 〇〇配列、出力: 〇〇オブジェクト】

【Thinkerへの要求事項】
1. SAVEPOINT.md に記載されたアーキテクチャ（MVMS、AppState、フェーズステートマシン）との整合性を検証してください[cite: 2]。
2. 他機能や他フェーズへの回帰バグ（副作用）リスクがないか、Chain of Thought（段階的思考）で分析してください[cite: 1]。
3. 変更対象ファイル（Target Files）を特定し、Coderへの具体的かつ純粋関数化を考慮した実装タスク指示をまとめてください[cite: 1, 2]。
4. 不明点や未定義仕様がある場合は推論で補完せず、一括で質問してください[cite: 1]。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。

.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: 不具合・エラーの根本原因特定および修正計画の策定】
発生現象:
- エラーメッセージ / ログ: 【例: TypeError: Cannot read properties of undefined (reading 'xxx')】
- 発生タイミング / 操作フロー: 【例: Tab1でデータ読み込み後、バリデーション実行時に発生】
- 想定動作と実動作の差異: 【例: 本来はPhase 4に遷移すべきところ、スクリプトが停止する】

【Thinkerへの要求事項】
1. エラー原因を推測に頼らず、コードの実行フローと単方向データフローの観点から根本原因を特定してください[cite: 1, 2]。
2. 修正による他フェーズ・モジュールへの影響度を精査してください[cite: 1]。
3. 変更対象ファイル、修正ロジック（モダン構文・null合体等の活用による安全策）、禁止事項を整理した実装計画書を作成してください[cite: 1, 2]。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。

.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: コードベースの最適化およびリファクタリング計画の策定】
対象範囲:
- 対象ファイル / モジュール: 【例: clasp/src/CoreMethod.html または 全体】
- 最適化の主眼: 【例: 冗長なガード条件の削減、命令的ループの高階関数（reduce等）化、純粋関数の厳格な分離】

【Thinkerへの要求事項】
1. 既存の機能要件・入出力・外部インターフェース（関数シグネチャ）の完全な互換性を維持できるか検証してください[cite: 1]。
2. SAVEPOINT.md のコーディング原則に基づき、オプショナルチェイニング（?.）、Null合体（??）、分割代入等によるコード削減箇所をリストアップしてください[cite: 1, 2]。
3. アクション内での命令的DOM操作を排除し、Stateパッチによるデータバインディングへ純粋化できる箇所を特定してください[cite: 2]。
4. Coderが迷わず置換作業を実行できる精緻なタスク分割書を作成してください。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。

.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: コメント規約およびフォーマットの厳格適用計画】
対象範囲:
- 対象ファイル: 【例: clasp/src/ 配下の全ファイル または 特定ファイル名】

【Thinkerへの要求事項】
1. 以下のコーディング規約に違反している箇所を網羅的に特定してください:
   - 関数ヘッダーが `// --- [Function Name] ---` 形式になっているか[cite: 1]
   - インラインコメントは「Why（理由・意図）」を説明し、日本語15文字以内になっているか[cite: 1]
   - コメント密度制限（10行あたり最大3件、20行あたり最大8件）を超過していないか[cite: 1]
   - クラス、メソッド、ブロック間に不要な空行（ブランク行）が存在しないか（完全削除の対象）[cite: 1, 2]
2. 修正対象行と、置換・削除すべきコメントの差分方針を実装計画書として整理してください。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。

plan.md の内容を承認（Approve）します。
20-coder-implementer.md の規約に従い、指定されたファイルのみにコード変更を適用してください。

.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: UI・UXの変更および宣言的バインディング調整計画の策定】
対象範囲:
- 対象画面 / コンポーネント: 【例: Tab1の抽出設定パネル または <ob-cal-plot> コンポーネント】
- 変更したいUI・UXの要件: 【例: エラー時のハイライト表示を追加し、フェーズ遷移に応じてボタンの活性/非活性を滑らかに切り替えたい】

【Thinkerへの要求事項】
1. 命令的なDOM操作（`element.style` や `innerHTML` の直接変更）を行わず、`AppState` の変更と `data-bind-*` を用いたPatch同期で実現する設計を提示してください。
2. 特定のフェーズ（State）に依存するUIの表示/非表示や活性状態は、分岐処理を散在させず `SCHEMA`（`activePhase`, `disablePhase`, `visiblePhase`）の定義更新として計画してください。
3. 既存のWeb Componentsの参照ロストを引き起こすような破壊的再構築（DOMの完全置き換え）のリスクがないか検証してください。
4. 変更対象ファイルと、Coder向けの実装タスク（SCHEMA更新、バインディング属性追加など）を整理してください。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。

.antigravity/ 配下のルール定義、clasp/docs/SAVEPOINT.md、および clasp/src/ 配下のコードベースを読み込んでください。

【タスク: アクション・パイプラインおよびフェーズ遷移の最適化計画の策定】
対象範囲:
- 対象フロー: 【例: Tab2におけるグラフ描画（FILTERED -> MAPPED -> PREVIEWED）のデータ処理フロー】
- 最適化の主眼: 【例: 連続する状態更新に伴う無駄な再描画の抑制、またはエラーハンドリングの共通化】

【Thinkerへの要求事項】
1. 現在の処理フローが、SAVEPOINT.md で定義されたステートマシン（状態遷移）において冗長、または不正な遷移をしていないか検証してください。
2. 確認ダイアログや非同期処理、ロック制御が絡むアクションについて、例外なく `CoreAction.confirmAndExecute` パイプラインに統合する設計を提示してください。
3. ユーザー操作やAPIレスポンスの処理において、アクション内で生データを無理に加工・DOM反映せず、UI用の「派生状態 (Derived State)」の計算へ分離（純粋関数化）できる箇所を洗い出してください。
4. フロー変更に伴う影響範囲（Target Files）と、Coder向けの具体的な修正指示を計画書としてまとめてください。

※00-pipeline-governance.mdに従い、コードの直接改変は行わず、結果を artifacts/plan.md に出力してユーザー承認待ち（ストッパー）としてください。
